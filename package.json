{
  "name": "pi-llama-metrics-display",
  "version": "1.0.0",
  "description": "Pi extension to display llama.cpp metrics (prefill speed, generation speed, progress)",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "install": "node install.js",
    "uninstall": "node uninstall.js"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
